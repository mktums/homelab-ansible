---
# Full homelab deploy: router + servers
#
# Usage:
#   just deploy                          # full deploy
#   just router                          # router only
#   just servers                         # servers only
#   just tags cyberchef                 # single service
#

- import_playbook: router.yml
- import_playbook: servers.yml
