---
- name: "System | Notify reboot required"
  ansible.builtin.debug:
    msg: "NOTE: Zswap configuration has been applied to GRUB. A manual reboot is required to activate it on {{ inventory_hostname }}."

- name: Reload systemd daemon
  ansible.builtin.systemd:
    daemon_reload: yes
