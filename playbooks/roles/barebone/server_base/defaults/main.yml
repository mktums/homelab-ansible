# BTRFS scrub schedule (disabled by default, override per-host)
system_btrfs_scrub_enabled: false

# Swap file (size computed dynamically from RAM in tuning.yml)
system_swap_enabled: true
system_swap_file: "/swapfile"
