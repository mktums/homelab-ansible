---
- name: Deploy DockHand agent
  block:
    - ansible.builtin.include_tasks: deploy.yml
    - ansible.builtin.include_tasks: init.yml
    - ansible.builtin.include_tasks: env.yml
  when: inventory_hostname in groups.get('dockhand_agent_hosts', [])
