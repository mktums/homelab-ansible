---
# Register CNAME after deployment (DNS must exist before services reference it)
- name: Register Meilisearch CNAME
  ansible.builtin.include_role:
    name: common_tasks
    tasks_from: register_cname
  vars:
    cname_name: "{{ meilisearch_cname }}.{{ lan_domain }}"
