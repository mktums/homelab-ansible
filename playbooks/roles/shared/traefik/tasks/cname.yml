---
- name: Register Traefik CNAME
  ansible.builtin.include_role:
    name: common_tasks
    tasks_from: register_cname
  vars:
    cname_name: "{{ traefik_cname }}.{{ inventory_hostname }}.{{ lan_domain }}"
