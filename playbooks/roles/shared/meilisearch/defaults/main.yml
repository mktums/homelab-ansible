# Meilisearch defaults

meilisearch_image_name: "getmeili/meilisearch"
meilisearch_image_tag: "latest"
meilisearch_port: 7700
meilisearch_opt_dir: "{{ ansible_opt_base }}/meilisearch"
meilisearch_data_dir: "{{ docker_data_base }}/meilisearch"

# CNAME subdomain (without .{{ lan_domain }})
meilisearch_cname: "meili"
