---
# Register all Docker hosts in DockHand (direct TLS)

- name: Register Docker hosts in DockHand
  ansible.builtin.include_tasks: register_single_host.yml
  loop: "{{ groups.get('docker_hosts', []) }}"
  loop_control:
    loop_var: _remote_host
