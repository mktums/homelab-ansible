---
all:
  hosts:
    router:
      ansible_host: "openwrt.{{ lan_domain }}"
      ansible_user: root
    lab1:
      ansible_host: "lab1.{{ lan_domain }}"
    lab2:
      ansible_host: "lab2.{{ lan_domain }}"

  children:
    routers:
      hosts:
        router:

    servers:
      hosts:
        lab1:
        lab2:

    docker_hosts:
      hosts:
        lab1:
        lab2:

    traefik_hosts:
      hosts:
        lab1:
        lab2:

    vaultwarden_hosts:
      hosts:
        lab1:

    qbittorrent_hosts:
      hosts:
        lab1:
        lab2:

    inpx_web_hosts:
      hosts:
        lab2:

    postgres_hosts:
      hosts:
        lab1:

    meilisearch_hosts:
      hosts:
        lab1:

    linkwarden_hosts:
      hosts:
        lab1:

    beszel_hub_hosts:
      hosts:
        lab2:

    beszel_agent_hosts:
      hosts:
        lab1:
        lab2:

    kopia_server_hosts:
      hosts:
        lab1:

    cyberchef_hosts:
      hosts:
        lab2:

    it_tools_hosts:
      hosts:
        lab2:

    kopia_agent_hosts:
      hosts:
        lab1:
        lab2:

    dockhand_hosts:
      hosts:
        lab2:

    dockhand_agent_hosts:
      hosts:
        lab1:

    homepage_hosts:
      hosts:
        lab2:

    arr_stack_hosts:
      hosts:
        lab1:

    jellyfin_hosts:
      hosts:
        lab1:

    immich_hosts:
      hosts:
        lab1:

    dbgate_hosts:
      hosts:
        lab1:

    searxng_hosts:
      hosts:
        lab2:
